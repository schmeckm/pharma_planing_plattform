# Backend seed data
# Source-of-truth mock data for the layered repository architecture.
# Run `npm run seed` to copy into the active data directory (HAP_DATA_DIR or ./data).

Seeds are copied from the project root `data/` folder during initial setup.
Use `npm run reset-data` to overwrite live JSON files with these seeds.
